import 'package:flutter/material.dart';

class AppColors {
  // Primary color (teal/turquoise) used in buttons and focused elements
  static Color primary = const Color(0xFF00BCD4);

  // Secondary color (pink) used in the logo
  static Color secondary = const Color(0xFFE91E63);

  // Accent color (yellow/orange) used in the logo
  static Color accent = const Color(0xFFFFA726);

  // Background color
  static Color background = Colors.white;

  // Text colors
  static Color textPrimary = const Color(0xFF333333);
  static Color textSecondary = const Color(0xFF757575);

  // Input field background color
  static Color inputBackground = const Color(0xFFF5F5F5);

  // Status colors
  static Color error = Colors.red;
  static Color success = Colors.green;

  // Disabled state colors
  static Color disabled = Colors.grey;

  // Divider and border colors
  static Color divider = const Color(0xFFEEEEEE);
  static Color border = const Color(0xFFE0E0E0);
}